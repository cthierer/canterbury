info:
  name: JWKS
  type: http
  seq: 1

http:
  method: GET
  url: "{{BaseURI}}/.well-known/jwks.json"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
