{
	"name": "canterbury-sync",
	"version": "1.0.0",
	"main": "index.js",
	"type": "module",
	"scripts": {
		"lint": "npm --prefix .. run lint:js",
		"lint:fix": "npm --prefix .. run lint:js -- --fix",
		"test": "node --test *.test.js",
		"check:code": "node --check sync.js && node --check worker.js && node --check health.js && npm test && npm run lint",
		"check": "npm run check:code && npm --prefix .. run format:check"
	},
	"author": "",
	"license": "UNLICENSED",
	"description": "",
	"dependencies": {
		"dotenv": "^17.4.2",
		"obsidian-headless": "0.0.13"
	},
	"devDependencies": {
		"@eslint/js": "^10.0.0",
		"eslint": "^10.0.0",
		"eslint-config-prettier": "^10.0.0",
		"globals": "^16.0.0"
	}
}
