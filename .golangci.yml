version: '2'

run:
  timeout: 5m
  tests: true

linters:
  default: standard
  enable:
    - errorlint
    - gosec
    - misspell
    - revive
    - unconvert
    - unparam
    - whitespace

formatters:
  enable:
    - gofmt
