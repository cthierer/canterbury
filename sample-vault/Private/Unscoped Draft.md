---
tags:
  - private
  - draft
status: draft
---

# Unscoped Draft

This note intentionally does not declare `access.scopes`.

It should be omitted from scoped search results and should return permission
denied through `ReadNote` when the service is enforcing the current access
model.
